<h1>트윗 전체목록</h1>
<%@tweets.each do |t|%>
  title <%=t.title%><br>
  content <%=t.content%><br>
  글쓴이 <%=t.user.email if t.user%><br>
  사진 : <%= image_tag t.photo_url%><br>
  <%=link_to "트윗보기", tweet_path(t.id)%>
  <hr>
<% end %>
<%= paginate(@tweets)%>
